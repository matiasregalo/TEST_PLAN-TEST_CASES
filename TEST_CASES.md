# CASOS DE PRUEBA 


## Matriz de Casos de Prueba

> Nota: Para este entregable documental, todos los casos quedan con resultado obtenido **Sin ejecutar** y estado **Sin ejecutar**.

| Historia de Usuario asociada | ID del Caso | Escenario en lenguaje Gherkin (Dado / Cuando / Entonces) | Precondiciones | Datos de entrada | Pasos de ejecucion | Resultado esperado | Resultado obtenido y Estado (Paso, fallo, sin ejecutar) | Prioridad |
|---|---|---|---|---|---|---|---|---|
| HU 3 - Gestionar y validar API Keys | TC-001 | <br>**Given** que soy Super Admin autenticado y el ecommerce existe sin claves previas<br>**When** creo una nueva API Key para ese ecommerce<br>**Then** el sistema genera la clave correctamente | Super Admin con sesion activa; ecommerce registrado; | ecommerceId valido | 1. Ingresar al modulo de API Keys<br>2. Seleccionar ecommerce valido<br>3. Ejecutar accion crear API Key<br>4. Consultar listado de claves | Se crea una nueva API Key y el ecommerce pasa a tener una clave más | Resultado obtenido: Sin ejecutar; Estado: Sin ejecutar | Critico |
| HU 3 - Gestionar y validar API Keys | TC-002 | <br>**Given** que soy Super Admin autenticado<br>**And** el ecommerce no existe<br>**When** intento crear una API Key<br>**Then** el sistema rechaza la operacion indicando ecommerce inexistente | Super Admin con sesion activa; ecommerce no registrado | ecommerceId inexistente | 1. Ir al modulo API Keys<br>2. Ingresar ecommerce inexistente<br>3. Intentar crear API Key | La creacion es rechazada con error 404 por ecommerce inexistente  | Resultado obtenido: Sin ejecutar; Estado: Sin ejecutar | Alto |
| HU 3 - Gestionar y validar API Keys | TC-003 | <br>**Given** que no soy Super Admin autenticado<br>**And** el ecommerce existe<br>**When** intento crear una API Key<br>**Then** el sistema rechaza la operacion por falta de permisos | Usuario sin rol Super Admin (o sin autenticacion valida); ecommerce existente | credenciales no autorizadas; ecommerceId valido | 1. Autenticar con usuario no autorizado<br>2. Intentar crear API Key para ecommerce valido | Operacion rechazada por autorizacion con error 401 y sin creacion de clave | Resultado obtenido: Sin ejecutar; Estado: Sin ejecutar | Critico |
| HU 3 - Gestionar y validar API Keys | TC-004 | <br>**Given** que soy Super Admin autenticado y el ecommerce existe con 1 clave<br>**When** consulto las API Keys del ecommerce<br>**Then** el sistema muestra la lista y enmascara la informacion sensible | Super Admin con sesion activa; ecommerce registrado; 1 API Key existente | ecommerceId valido | 1. Entrar al modulo API Keys<br>2. Seleccionar ecommerce con 1 clave<br>3. Ejecutar consulta de claves | Se retorna lista con 1 clave y el valor sensible aparece parcialmente oculto | Resultado obtenido: Sin ejecutar; Estado: Sin ejecutar | Critico |
| HU 3 - Gestionar y validar API Keys | TC-005 | <br>**Given** que soy Super Admin autenticado y el ecommerce existe sin claves<br>**When** consulto las API Keys del ecommerce<br>**Then** el sistema responde lista vacia sin mostrar ningún error | Super Admin con sesion activa; ecommerce registrado; 0 claves | ecommerceId valido | 1. Entrar al modulo API Keys<br>2. Seleccionar ecommerce sin claves<br>3. Consultar listado | Respuesta exitosa con lista vacia y sin exponer datos sensibles | Resultado obtenido: Sin ejecutar; Estado: Sin ejecutar | Medio |
| HU 3 - Gestionar y validar API Keys | TC-006 | <br>**Given** que soy Super Admin autenticado<br>**And** el ecommerce no existe<br>**When** consulto las API Keys<br>**Then** el sistema rechaza la consulta indicando ecommerce inexistente | Super Admin con sesion activa; ecommerce no registrado | ecommerceId inexistente | 1. Ingresar al modulo API Keys<br>2. Intentar consultar con ecommerce inexistente | Consulta rechazada con error 404  | Resultado obtenido: Sin ejecutar; Estado: Sin ejecutar | Alto |
| HU 3 - Gestionar y validar API Keys | TC-007 | <br>**Given** que no soy Super Admin autenticado<br>**And** el ecommerce existe<br>**When** consulto las API Keys del ecommerce<br>**Then** el sistema rechaza la operacion por autorizacion | Usuario sin rol Super Admin; ecommerce existente | credenciales no autorizadas; ecommerceId valido | 1. Autenticar con usuario no autorizado<br>2. Intentar consultar claves del ecommerce | Consulta rechazada por autorizacion y error 401 | Resultado obtenido: Sin ejecutar; Estado: Sin ejecutar | Critico |
| HU 7 - Definir el tope maximo y la prioridad de descuentos | TC-008 | <br>**Given** existen tipos de descuento disponibles en el ecommerce<br>**And** el tope maximo propuesto es un valor positivo valido (dentro del rango permitido)<br>**And** la prioridad define un orden unico: Temporada=1, Fidelidad=2, Tipo de producto=3<br>**When** se registra la configuracion de topes y prioridad<br>**Then** la configuracion queda vigente para el ecommerce<br>**And** el motor utiliza ese orden para resolver descuentos concurrentes | Usuario de LOYALTY autenticado en el Dashboard (http://localhost:3000); tipos de descuento registrados en el ecommerce; sin configuracion previa conflictiva | tope = 20% / tope = 0.01% (limite inferior) / tope = 100% (limite superior); prioridades = [Temporada: 1, Fidelidad: 2, Tipo de producto: 3] | 1. Acceder al modulo de configuracion de descuentos<br>2. Ingresar tope maximo = 20% (primera iteracion), tope = 0.01% (segunda iteracion) o tope = 100% (tercera iteracion)<br>3. Asignar prioridades unicas a cada tipo de descuento<br>4. Guardar configuracion<br>5. Verificar que la configuracion queda vigente y que el motor la utiliza | La configuracion se guarda correctamente en las tres iteraciones; el motor aplica descuentos respetando el orden [Temporada > Fidelidad > Tipo de producto]; el sistema acepta el limite inferior (0.01%) y el limite superior (100%) como valores validos | Resultado obtenido: Sin ejecutar; Estado: Sin ejecutar | Critico |
| HU 7 - Definir el tope maximo y la prioridad de descuentos | TC-009 | <br>**Given** existen limites de negocio para proteger la rentabilidad<br>**And** existe una configuracion previa valida<br>**When** se intenta registrar un tope maximo con valor invalido (0%, negativo o mayor a 100%)<br>**Then** la configuracion es rechazada<br>**And** se mantiene la ultima configuracion valida | Usuario de LOYALTY autenticado en el Dashboard (http://localhost:3000); existe configuracion previa guardada con tope valido | tope = 0% / tope = -15% / tope = 100.01% | 1. Acceder al modulo de configuracion de descuentos<br>2. Ingresar tope maximo = 0% (primera iteracion), tope = -15% (segunda iteracion) o tope = 100.01% (tercera iteracion)<br>3. Intentar guardar la configuracion<br>4. Verificar que la configuracion anterior permanece sin modificar | La operacion es rechazada con error de validacion en las tres iteraciones (tope fuera del rango permitido); la configuracion previa permanece intacta | Resultado obtenido: Sin ejecutar; Estado: Sin ejecutar | Critico |
| HU 7 - Definir el tope maximo y la prioridad de descuentos | TC-010 | <br>**Given** la prioridad de descuentos debe ser deterministica<br>**And** existe una configuracion de prioridad previa valida<br>**When** se intenta guardar una configuracion con prioridades duplicadas en cualquier nivel, con o sin tope invalido<br>**Then** la configuracion es rechazada<br>**And** no se altera la configuracion vigente | Usuario de LOYALTY autenticado en el Dashboard (http://localhost:3000); existe configuracion previa valida con tope y prioridades vigentes | Iter 1: prioridades = [Temporada: 1, Fidelidad: 1, Tipo de producto: 2] (duplicado en nivel 1)<br>Iter 2: prioridades = [Temporada: 1, Fidelidad: 2, Tipo de producto: 2] (empate en nivel intermedio)<br>Iter 3: tope = 30% (valido); prioridades = [Temporada: 1, Fidelidad: 1, Tipo de producto: 3] (duplicado en nivel 1)<br>Iter 4: tope = 0% (invalido); prioridades = [Temporada: 1, Fidelidad: 1, Tipo de producto: 2] (duplicado en nivel 1) | 1. Acceder al modulo de configuracion de descuentos<br>2. Ingresar datos segun la iteracion correspondiente (tope y/o prioridades con duplicados)<br>3. Intentar guardar la configuracion<br>4. Verificar que la operacion es rechazada con el mensaje de error correspondiente<br>5. Verificar que la configuracion previa permanece sin modificar | La operacion es rechazada en las cuatro iteraciones; en las iter 1, 2 y 3 se indica prioridad ambigua; en la iter 4 se indica tanto tope invalido como prioridad ambigua; la configuracion vigente no cambia en ninguna iteracion | Resultado obtenido: Sin ejecutar; Estado: Sin ejecutar | Critico |
| HU 7 - Definir el tope maximo y la prioridad de descuentos | TC-011 | <br>**Given** existe una configuracion vigente con tope = 20% y prioridades unicas<br>**And** una transaccion califica para multiples descuentos con total acumulado = 35% (descuento temporada 15% + fidelidad 10% + tipo producto 10%)<br>**When** el motor calcula el descuento total<br>**Then** el descuento final aplicado se limita al 20% (tope maximo) | Usuario de LOYALTY autenticado en el Dashboard (http://localhost:3000); configuracion vigente con tope = 20%; reglas activas de temporada (15%), fidelidad (10%) y tipo de producto (10%) | descuento temporada = 15%; descuento fidelidad = 10%; descuento tipo producto = 10%; tope configurado = 20% | 1. Iniciar sesion como usuario de LOYALTY en el Dashboard (http://localhost:3000)<br>2. Navegar al modulo de configuracion de descuentos y verificar que el tope vigente es 20% y las prioridades estan configuradas con orden unico<br>3. Confirmar desde el Dashboard que las reglas activas de temporada (15%), fidelidad (10%) y tipo de producto (10%) estan vigentes<br>4. Enviar POST al endpoint del motor con una transaccion que califica para los 3 tipos de descuento<br>5. Verificar en los logs de auditoria del Dashboard y en la respuesta del motor que el descuento final es 20% (tope maximo) y no 35% (acumulado bruto) | El descuento acumulado (35%) es limitado por el tope maximo; el descuento final aplicado es exactamente 20% | Resultado obtenido: Sin ejecutar; Estado: Sin ejecutar | Critico |
| HU 7 - Definir el tope maximo y la prioridad de descuentos | TC-012 | <br>**Given** existe una configuracion vigente con prioridades unicas<br>**And** una transaccion califica para multiples descuentos con total acumulado menor o igual al tope configurado<br>**When** el motor calcula el descuento total<br>**Then** el descuento final aplicado es el valor acumulado integro, sin ningun recorte | Usuario de LOYALTY autenticado en el Dashboard (http://localhost:3000); configuracion vigente con tope definido; reglas activas que generan descuento acumulado <= tope | Iter 1: descuento acumulado = 25%; tope configurado = 25% (acumulado igual al tope)<br>Iter 2: descuento acumulado = 15%; tope configurado = 30% (acumulado por debajo del tope) | 1. Iniciar sesion como usuario de LOYALTY en el Dashboard (http://localhost:3000)<br>2. Navegar al modulo de configuracion de descuentos y verificar el tope correspondiente a la iteracion (25% en Iter 1; 30% en Iter 2)<br>3. Confirmar desde el Dashboard que las reglas activas generan el acumulado indicado por iteracion<br>4. Enviar POST al endpoint del motor con una transaccion que califica para los descuentos de la iteracion correspondiente<br>5. Verificar en los logs de auditoria del Dashboard y en la respuesta del motor que el descuento final coincide con el acumulado integro sin recorte en ambas iteraciones | En ambas iteraciones el descuento acumulado se aplica integro; en la iter 1 (25% = 25%) no hay recorte en el valor exacto del tope; en la iter 2 (15% < 30%) tampoco se aplica ninguna limitacion | Resultado obtenido: Sin ejecutar; Estado: Sin ejecutar | Critico |
| HU 8 - Clasificar al cliente segun el payload recibido | TC-013 | <br>**Given** existe una matriz de clasificacion vigente con fronteras Bronce/Plata en totalCompras=10/11 y Plata/Oro en totalCompras=50/51<br>**And** el payload contiene todos los atributos requeridos con totalCompras igual al valor limite de cada frontera<br>**When** el motor evalua el payload del cliente<br>**Then** el cliente queda asignado al nivel que corresponde exactamente al limite evaluado<br>**And** no se produce asignacion al nivel adyacente | Motor activo; API Key valida; matriz de clasificacion vigente con Bronce [0-10], Plata [11-50] y Oro [51+]; cuatro iteraciones con totalCompras en los extremos de ambas fronteras | Iter 1: idCliente="CLI-002"; totalCompras=10 (limite superior Bronce); montoTotalGastado=300.00; mesesComoCliente=6<br>Iter 2: idCliente="CLI-003"; totalCompras=11 (limite inferior Plata); montoTotalGastado=350.00; mesesComoCliente=7<br>Iter 3: idCliente="CLI-004"; totalCompras=50 (limite superior Plata); montoTotalGastado=3800.00; mesesComoCliente=24<br>Iter 4: idCliente="CLI-005"; totalCompras=51 (limite inferior Oro); montoTotalGastado=3900.00; mesesComoCliente=25 | 1. Verificar la configuracion de la matriz vigente<br>2. Enviar POST con el payload de la Iter 1 (totalCompras=10) y registrar el nivel retornado<br>3. Enviar POST con el payload de la Iter 2 (totalCompras=11) y registrar el nivel retornado<br>4. Enviar POST con el payload de la Iter 3 (totalCompras=50) y registrar el nivel retornado<br>5. Enviar POST con el payload de la Iter 4 (totalCompras=51) y registrar el nivel retornado<br>6. Comparar los cuatro resultados confirmando que el nivel cambia exactamente en cada frontera | Iter 1 (totalCompras=10): HTTP 200; nivel="Bronce" (no Plata)<br>Iter 2 (totalCompras=11): HTTP 200; nivel="Plata" (no Bronce)<br>Iter 3 (totalCompras=50): HTTP 200; nivel="Plata" (no Oro)<br>Iter 4 (totalCompras=51): HTTP 200; nivel="Oro" (no Plata); las cuatro iteraciones confirman las fronteras exactas entre niveles adyacentes  | Resultado obtenido: Sin ejecutar; Estado: Sin ejecutar | Critico |
| HU 8 - Clasificar al cliente segun el payload recibido | TC-014 | <br>**Given** existe una matriz de clasificacion vigente con Bronce como nivel base desde totalCompras=0<br>**And** el payload contiene todos los atributos requeridos con totalCompras=0 (minimo absoluto del dominio valido)<br>**When** el motor evalua el payload del cliente<br>**Then** el cliente queda asignado al nivel Bronce<br>**And** la solicitud no es rechazada por valor fuera de dominio | Motor activo; API Key valida; matriz de clasificacion vigente que incluye totalCompras=0 como valor valido dentro del nivel Bronce | idCliente="CLI-006"; totalCompras=0 (limite inferior absoluto del dominio); montoTotalGastado=0.00; mesesComoCliente=0 | 1. Verificar que la matriz vigente incluye totalCompras=0 como valor valido en el nivel Bronce<br>2. Enviar POST al endpoint de clasificacion con totalCompras=0<br>3. Verificar el codigo de respuesta HTTP<br>4. Verificar que el nivel asignado es Bronce y que no se retorna error de validacion | HTTP 200; nivel="Bronce"; el valor minimo absoluto del dominio es aceptado como valido y clasificado correctamente sin error  | Resultado obtenido: Sin ejecutar; Estado: Sin ejecutar | Alto |
| HU 8 - Clasificar al cliente segun el payload recibido | TC-015 | <br>**Given** existen reglas de validacion que restringen totalCompras al dominio [0, +inf)<br>**And** el payload contiene totalCompras=-1, valor justo por debajo del limite inferior del dominio valido<br>**When** el motor evalua el payload del cliente<br>**Then** la clasificacion es rechazada<br>**And** no se asigna ningun nivel de descuento al cliente | Motor activo; API Key valida; matriz de clasificacion vigente | idCliente="CLI-007"; totalCompras=-1 (un paso por debajo del limite inferior del dominio); montoTotalGastado=100.00; mesesComoCliente=2 | 1. Enviar POST al endpoint de clasificacion con totalCompras=-1<br>2. Verificar el codigo de respuesta HTTP<br>3. Verificar que no se retorna ningun nivel en la respuesta<br>4. Verificar que el mensaje de error indica que totalCompras es invalido | HTTP 400; el cuerpo de respuesta indica que totalCompras tiene un valor fuera del dominio valido; ningun nivel asignado | Resultado obtenido: Sin ejecutar; Estado: Sin ejecutar | Critico |
| HU 8 - Clasificar al cliente segun el payload recibido | TC-016 | <br>**Given** existen atributos obligatorios para clasificar al cliente<br>**And** el payload recibido omite uno o todos los atributos obligatorios<br>**When** el motor evalua el payload del cliente<br>**Then** la clasificacion es rechazada<br>**And** se informa que no es posible determinar el nivel de descuento | Motor activo; API Key valida; matriz de clasificacion vigente; tres iteraciones con payload que omite atributos obligatorios | Iter 1: payload sin totalCompras (idCliente="CLI-008"; montoTotalGastado=200.00; mesesComoCliente=4)<br>Iter 2: payload sin idCliente (totalCompras=15; montoTotalGastado=700.00; mesesComoCliente=10)<br>Iter 3: payload completamente vacio {} | 1. Enviar POST con el payload de la Iter 1 (sin totalCompras) y verificar respuesta<br>2. Enviar POST con el payload de la Iter 2 (sin idCliente) y verificar respuesta<br>3. Enviar POST con el payload de la Iter 3 ({}) y verificar respuesta<br>4. En las tres iteraciones verificar que no se retorna nivel de fidelidad<br>5. Verificar que el mensaje indica que el/los atributo(s) obligatorio(s) estan ausentes | HTTP 400 en las tres iteraciones; en las iter 1 y 2 la respuesta especifica cual atributo obligatorio esta ausente; en la iter 3 lista todos los atributos obligatorios ausentes; en ninguna iteracion se asigna nivel ni se puede determinar el nivel de descuento  | Resultado obtenido: Sin ejecutar; Estado: Sin ejecutar | Critico |
| HU 8 - Clasificar al cliente segun el payload recibido | TC-017 | <br>**Given** existen reglas de validacion de tipo de dato para los atributos del payload<br>**And** el payload contiene un valor de tipo incorrecto en un campo numerico<br>**When** el motor evalua el payload del cliente<br>**Then** la clasificacion es rechazada<br>**And** no se asigna nivel de descuento al cliente | Motor activo; API Key valida; matriz de clasificacion vigente; dos iteraciones con tipo de dato incorrecto en el campo totalCompras | Iter 1: idCliente="CLI-009"; totalCompras="veinte" (string en campo entero); montoTotalGastado=600.00; mesesComoCliente=8<br>Iter 2: idCliente="CLI-010"; totalCompras=true (booleano en campo entero); montoTotalGastado=400.00; mesesComoCliente=5 | 1. Enviar POST con el payload de la Iter 1 (totalCompras="veinte") y verificar respuesta<br>2. Enviar POST con el payload de la Iter 2 (totalCompras=true) y verificar respuesta<br>3. Verificar en ambas iteraciones que no se asigna nivel y que el mensaje indica tipo de dato invalido | HTTP 400 en ambas iteraciones; el mensaje indica que el tipo de dato del atributo es invalido para el dominio definido; ningun nivel asignado en ninguna iteracion | Resultado obtenido: Sin ejecutar; Estado: Sin ejecutar | Medio |
| HU 8 - Clasificar al cliente segun el payload recibido | TC-018 | <br>**Given** existe una configuracion de clasificacion vigente sin modificaciones entre evaluaciones<br>**And** el mismo payload valido es enviado dos veces consecutivas al motor<br>**When** el motor evalua el payload en la primera ocasion<br>**And** el motor evalua el mismo payload en la segunda ocasion sin cambios en la configuracion<br>**Then** el nivel de fidelidad asignado es identico en ambas evaluaciones<br>**And** no existe variacion ni indeterminismo en la clasificacion | Motor activo; API Key valida; matriz de clasificacion vigente sin modificaciones entre las dos evaluaciones | Primera evaluacion y segunda evaluacion (identicas): idCliente="CLI-001"; totalCompras=25; montoTotalGastado=1500.00; mesesComoCliente=18 | 1. Enviar POST con el payload de referencia (totalCompras=25) y registrar el nivel retornado en la primera evaluacion<br>2. Sin modificar la configuracion de la matriz, enviar exactamente el mismo POST nuevamente<br>3. Registrar el nivel retornado en la segunda evaluacion<br>4. Comparar los dos resultados verificando que son identicos | HTTP 200 en ambas evaluaciones; nivel="Plata" en la primera evaluacion y nivel="Plata" en la segunda evaluacion; los resultados son identicos, confirmando el determinismo del motor de clasificacion | Resultado obtenido: Sin ejecutar; Estado: Sin ejecutar | Critico |
| HU 8 - Clasificar al cliente segun el payload recibido | TC-019 | <br>**Given** existe una matriz de clasificacion vigente con tres niveles diferenciados<br>**And** se evaluan cuatro combinaciones de atributos: tres pertenecientes a clases validas de niveles distintos y una invalida por atributo ausente<br>**When** el motor evalua cada payload<br>**Then** cada combinacion valida es asignada al nivel de fidelidad que corresponde segun la tabla de decision<br>**And** la combinacion invalida es rechazada sin asignar nivel | Motor activo; API Key valida; matriz de clasificacion vigente con Bronce [0-10], Plata [11-50] y Oro [51+] en totalCompras | Iter 1: idCliente="CLI-011"; totalCompras=5; montoTotalGastado=100.00; mesesComoCliente=2 (representativa del nivel Bronce)<br>Iter 2: idCliente="CLI-012"; totalCompras=30; montoTotalGastado=2000.00; mesesComoCliente=14 (representativa del nivel Plata)<br>Iter 3: idCliente="CLI-013"; totalCompras=75; montoTotalGastado=6000.00; mesesComoCliente=36 (representativa del nivel Oro)<br>Iter 4: totalCompras=null; montoTotalGastado=500.00; mesesComoCliente=5 (atributo obligatorio ausente — invalido) | 1. Enviar POST con el payload de la Iter 1 (totalCompras=5) y verificar nivel Bronce<br>2. Enviar POST con el payload de la Iter 2 (totalCompras=30) y verificar nivel Plata<br>3. Enviar POST con el payload de la Iter 3 (totalCompras=75) y verificar nivel Oro<br>4. Enviar POST con el payload de la Iter 4 (totalCompras=null) y verificar rechazo<br>5. Comparar todos los resultados con la tabla de decision esperada | Iter 1: HTTP 200; nivel="Bronce"<br>Iter 2: HTTP 200; nivel="Plata"<br>Iter 3: HTTP 200; nivel="Oro"<br>Iter 4: HTTP 400; clasificacion rechazada; ningun nivel asignado; cada fila de la tabla de decision activa el resultado esperado | Resultado obtenido: Sin ejecutar; Estado: Sin ejecutar | Critico |
| HU 9 - Enviar el carrito y recibir el precio final con descuentos aplicados | TC-020 | <br>**Given** el ecommerce esta autorizado con API Key valida<br>**And** el carrito contiene items validos con subtotal = $200.00<br>**And** existen reglas de descuento vigentes que acumulan 35% (temporada 15% + fidelidad 10% + tipo producto 10%)<br>**And** existe una configuracion vigente con tope = 20% y prioridades unicas [Temporada=1, Fidelidad=2, Tipo producto=3]<br>**When** el ecommerce solicita el calculo del carrito<br>**Then** el precio final retornado es $160.00 (subtotal limitado al tope 20%)<br>**And** el descuento aplicado es exactamente 20%<br>**And** el orden de aplicacion respeta la prioridad configurada | Motor activo; ecommerce con API Key valida; carrito con 3 items validos; reglas activas de temporada (15%), fidelidad (10%) y tipo de producto (10%); configuracion vigente con tope = 20% y prioridades unicas | header x-api-key valida; idCarrito="CART-001"; items=[{id:"P001", precio:50.00, cantidad:2},{id:"P002", precio:100.00, cantidad:1}]; subtotal=200.00; descuento acumulado=35%; tope configurado=20% | 1. Autenticar la solicitud con API Key valida en el header x-api-key<br>2. Enviar POST al endpoint de calculo con el payload del carrito<br>3. Verificar el codigo de respuesta HTTP<br>4. Verificar que el precio final en la respuesta es $160.00<br>5. Verificar que el descuento total aplicado es 20% (tope) y no 35% (acumulado bruto)<br>6. Verificar que la respuesta refleja el orden de aplicacion segun la prioridad configurada | HTTP 200; precio final = $160.00; descuento aplicado = 20% (el tope limita el acumulado de 35%); la respuesta indica que el descuento fue recortado por el tope maximo; el orden de aplicacion respeta la prioridad [Temporada > Fidelidad > Tipo producto] | Resultado obtenido: Sin ejecutar; Estado: Sin ejecutar | Critico |
| HU 9 - Enviar el carrito y recibir el precio final con descuentos aplicados | TC-021 | <br>**Given** el ecommerce esta autorizado con API Key valida<br>**And** el carrito contiene items validos con subtotal = $100.00<br>**And** existe una configuracion vigente con tope = 25%<br>**And** el descuento acumulado de las reglas vigentes es exactamente 25% (igual al tope)<br>**When** el ecommerce solicita el calculo del carrito<br>**Then** el precio final retornado es $75.00<br>**And** el descuento aplicado es 25% integro sin ningun recorte | Motor activo; ecommerce con API Key valida; configuracion vigente con tope = 25%; reglas de descuento ajustadas para producir acumulado exactamente igual al tope; subtotal = $100.00 | header x-api-key valida; idCarrito="CART-009"; items validos; subtotal=$100.00; descuento acumulado=25%; tope configurado=25% | 1. Verificar configuracion vigente con tope = 25%<br>2. Enviar POST al endpoint de calculo con carrito que genera descuento acumulado = 25%<br>3. Verificar el codigo de respuesta HTTP<br>4. Verificar que el precio final en la respuesta es $75.00<br>5. Verificar que el descuento aplicado es 25% integro | HTTP 200; precio final = $75.00; descuento aplicado = 25% sin recorte; el sistema acepta el valor exacto del tope como acumulado valido e integro, sin tratarlo como excedente | Resultado obtenido: Sin ejecutar; Estado: Sin ejecutar | Critico |
| HU 9 - Enviar el carrito y recibir el precio final con descuentos aplicados | TC-022 | <br>**Given** el ecommerce esta autorizado con API Key valida<br>**And** el carrito contiene items validos con subtotal = $150.00<br>**And** no existen reglas de descuento aplicables para ninguno de los items del carrito<br>**When** el ecommerce solicita el calculo del carrito<br>**Then** el precio final retornado es igual al subtotal del carrito ($150.00)<br>**And** la respuesta indica que no se aplico ningun descuento | Motor activo; ecommerce con API Key valida; carrito valido cuyos items no califican para ninguna regla de descuento vigente; tope y prioridades configurados | header x-api-key valida; idCarrito="CART-002"; items=[{id:"P010", precio:75.00, cantidad:2}]; subtotal=150.00; ninguna regla de descuento activa aplica a P010 | 1. Autenticar la solicitud con API Key valida<br>2. Enviar POST al endpoint de calculo con el carrito que no califica para descuentos<br>3. Verificar el codigo de respuesta HTTP<br>4. Verificar que el precio final coincide con el subtotal<br>5. Verificar que la respuesta indica descuento total = 0 o lista de descuentos aplicados vacia | HTTP 200; precio final = $150.00 (igual al subtotal sin modificacion); descuento total = 0%; la respuesta indica explicitamente que no hubo descuentos aplicados al carrito | Resultado obtenido: Sin ejecutar; Estado: Sin ejecutar | Alto |
| HU 9 - Enviar el carrito y recibir el precio final con descuentos aplicados | TC-023 | <br>**Given** el servicio requiere autenticacion S2S mediante API Key valida<br>**And** el carrito a enviar es valido y completo<br>**When** el ecommerce realiza la solicitud de calculo sin credencial o con credencial invalida<br>**Then** la solicitud es rechazada por autenticacion<br>**And** no se retorna precio final en ninguna iteracion | Carrito valido y completo para ambas iteraciones; Iter 1: sin header x-api-key en la solicitud; Iter 2: API Key revocada o inexistente en el sistema | Iter 1: carrito completo (idCarrito="CART-003"; items validos; subtotal=200.00); solicitud enviada sin header x-api-key<br>Iter 2: mismo carrito completo; header x-api-key="REVOKED-KEY-999" | 1. Preparar el carrito valido de referencia<br>2. Enviar POST sin incluir el header x-api-key (Iter 1) y registrar respuesta<br>3. Enviar POST con API Key revocada en el header (Iter 2) y registrar respuesta<br>4. En ambas iteraciones verificar que no se retorna precio final<br>5. Verificar el codigo de error HTTP y el mensaje descriptivo en cada iteracion | HTTP 401 en ambas iteraciones; solicitud rechazada por autenticacion; sin precio final en la respuesta; en Iter 1 el mensaje indica ausencia de credencial; en Iter 2 el mensaje indica que la API Key es invalida o se encuentra revocada | Resultado obtenido: Sin ejecutar; Estado: Sin ejecutar | Critico |
| HU 9 - Enviar el carrito y recibir el precio final con descuentos aplicados | TC-024 | <br>**Given** el servicio requiere estructura minima del payload del carrito con campos obligatorios<br>**And** el payload enviado omite uno o mas campos obligatorios<br>**When** el ecommerce envia el carrito con la estructura incompleta<br>**Then** la solicitud es rechazada<br>**And** no se retorna precio final hasta contar con un carrito valido | Motor activo; ecommerce con API Key valida; tres iteraciones con campos obligatorios ausentes en el payload | Iter 1: payload sin lista de items (idCarrito="CART-004"; subtotal=200.00; sin campo items)<br>Iter 2: payload sin idCarrito (items=[{id:"P001", precio:50.00, cantidad:2}]; subtotal=100.00; sin campo idCarrito)<br>Iter 3: payload completamente vacio {} | 1. Autenticar la solicitud con API Key valida<br>2. Enviar POST con el payload de Iter 1 (sin items) y verificar respuesta<br>3. Enviar POST con el payload de Iter 2 (sin idCarrito) y verificar respuesta<br>4. Enviar POST con el payload de Iter 3 ({}) y verificar respuesta<br>5. En las tres iteraciones verificar que no se retorna precio final<br>6. Verificar que el mensaje de error especifica el campo obligatorio ausente | HTTP 400 en las tres iteraciones; en Iter 1 y 2 la respuesta especifica el campo obligatorio ausente; en Iter 3 lista todos los campos obligatorios ausentes; en ninguna iteracion se retorna precio final ni se invoca el motor de calculo | Resultado obtenido: Sin ejecutar; Estado: Sin ejecutar | Critico |
| HU 9 - Enviar el carrito y recibir el precio final con descuentos aplicados | TC-025 | <br>**Given** el servicio valida que los datos de cada item del carrito se encuentren dentro del dominio valido<br>**And** el payload enviado contiene valores fuera de dominio en los campos numericos del item<br>**When** el ecommerce envia el carrito con datos inconsistentes<br>**Then** la solicitud es rechazada<br>**And** no se retorna precio final | Motor activo; ecommerce con API Key valida; cuatro iteraciones con valores en los limites invalidos de precio y cantidad por item | Iter 1: item con precio=0.00 (limite inferior invalido; precio debe ser > 0); cantidad=1; idCarrito="CART-005"<br>Iter 2: item con precio=-10.00 (precio negativo; un paso por debajo del dominio valido); cantidad=1; idCarrito="CART-006"<br>Iter 3: item con precio=50.00; cantidad=0 (limite inferior invalido; cantidad debe ser >= 1); idCarrito="CART-007"<br>Iter 4: item con precio=50.00; cantidad=-1 (cantidad negativa; un paso por debajo del dominio valido); idCarrito="CART-008" | 1. Autenticar la solicitud con API Key valida<br>2. Enviar POST con item de precio=0.00 (Iter 1) y verificar respuesta<br>3. Enviar POST con item de precio=-10.00 (Iter 2) y verificar respuesta<br>4. Enviar POST con item de cantidad=0 (Iter 3) y verificar respuesta<br>5. Enviar POST con item de cantidad=-1 (Iter 4) y verificar respuesta<br>6. Verificar en todas las iteraciones que no se retorna precio final y que el mensaje indica el campo invalido | HTTP 400 en las cuatro iteraciones; el mensaje de error indica el campo del item que contiene el valor fuera de dominio (precio o cantidad); sin precio final retornado; la validacion del payload rechaza los datos inconsistentes antes de invocar el motor de calculo | Resultado obtenido: Sin ejecutar; Estado: Sin ejecutar | Critico |
| HU 9 - Enviar el carrito y recibir el precio final con descuentos aplicados | TC-026 | <br>**Given** existen cuatro combinaciones posibles de las condiciones del servicio: autenticacion, validez del carrito y existencia de descuentos aplicables<br>**And** cada combinacion produce un resultado distinto segun la tabla de decision<br>**When** se ejecuta una solicitud bajo las condiciones de cada regla<br>**Then** el sistema retorna el resultado correspondiente a la combinacion activa en cada iteracion | Motor activo; Iter 1: API Key valida + carrito valido + descuentos aplicables + acumulado < tope; Iter 2: sin API Key + carrito valido; Iter 3: API Key valida + carrito invalido (sin items); Iter 4: API Key valida + carrito valido + sin descuentos aplicables | Iter 1 (Regla 1 — camino feliz con descuento): API Key valida; idCarrito="CART-010"; items validos; subtotal=$200.00; descuento acumulado=15%; tope configurado=25%<br>Iter 2 (Regla 2 — sin autenticacion): sin header x-api-key; idCarrito="CART-010"; items validos<br>Iter 3 (Regla 3 — carrito invalido): API Key valida; payload sin campo items (idCarrito="CART-010"; subtotal=200.00)<br>Iter 4 (Regla 4 — sin descuentos aplicables): API Key valida; idCarrito="CART-011"; items validos; subtotal=$200.00; sin reglas de descuento aplicables a esos items | 1. Enviar POST bajo las condiciones de Iter 1 (API Key valida + carrito valido + descuentos aplicables) y registrar respuesta<br>2. Enviar POST bajo las condiciones de Iter 2 (sin API Key) y registrar respuesta<br>3. Enviar POST bajo las condiciones de Iter 3 (carrito invalido) y registrar respuesta<br>4. Enviar POST bajo las condiciones de Iter 4 (sin descuentos aplicables) y registrar respuesta<br>5. Comparar cada resultado con la tabla de decision verificando que la regla activa produce el resultado esperado | Iter 1 (R1): HTTP 200; precio final = $170.00 (descuento 15% aplicado integro por ser menor al tope 25%)<br>Iter 2 (R2): HTTP 401; solicitud rechazada por autenticacion; sin precio final<br>Iter 3 (R3): HTTP 400; solicitud rechazada por carrito invalido; sin precio final<br>Iter 4 (R4): HTTP 200; precio final = $200.00 (igual al subtotal; descuento = 0%); cada regla de la tabla de decision activa el resultado correcto | Resultado obtenido: Sin ejecutar; Estado: Sin ejecutar | Critico |

---